To run: first have jupiter notebook installed on computer (https://jupyter.org/install) then simply open the loan prediction.ipynb file

In order to see the decision tree make sure pydotplus is installed by running conda 
$ conda install -c conda-forge pydotplus
